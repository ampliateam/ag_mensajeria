export type TPackMensajeria = {
  correo: {
    totalHistorico: number,
    disponible: number,
  };
  sms: {
    totalHistorico: number,
    disponible: number,
  };
  whatsapp: {
    totalHistorico: number,
    disponible: number,
  };
};
